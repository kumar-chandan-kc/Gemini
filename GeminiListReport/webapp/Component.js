jQuery.sap.declare("GeminiSmartTemplate.Component");
sap.ui.getCore().loadLibrary("sap.ui.generic.app");
jQuery.sap.require("sap.ui.generic.app.AppComponent");

sap.ui.generic.app.AppComponent.extend("GeminiSmartTemplate.Component", {
	metadata: {
		"manifest": "json"
	}
});